$MEM-GC

val mui8 age = 15.
val mf32 height = 1.75.
val cstr name = "Simar".
val mbln active = true.

val mui8 result = 5 + 3.
val mui8 calculation = (10 + 5) * 2.

val mui8 empty.

fc main(a, x, b, n)!void {
    val mui8 age = 0.
}